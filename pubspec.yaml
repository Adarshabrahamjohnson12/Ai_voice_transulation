name: ai_voice_translator
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0
  


dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^7.0.0          # For voice input
  translator: ^1.0.3+1 
  flutter_tts: ^4.2.2
  provider: ^6.1.1
  permission_handler: ^12.0.0+1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

 
  flutter_lints: ^5.0.0

flutter:

  
  uses-material-design: true
